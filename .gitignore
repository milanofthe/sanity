node_modules/
dist/
target/
.DS_Store
*.log
web/shots/
artifacts/
# Fixtures dumped from other repositories by `cargo run -p sanity-core
# --example dump`. Several megabytes each, regenerated in one command, and
# what is in them is somebody else's code rather than this project's. The rest
# of web/public is served as it is, the favicon among it, so only the dumps
# are ignored.
web/public/fixture*/
# The web demo's baked repositories, generated by scripts/demo.mjs and by the
# workflow that deploys the site, plus the clones they are dumped from.
web/public/demo/
.demo-src/
# Local agent settings and session state.
.claude/

# npm packages, assembled by scripts/npm-packages.mjs
/dist/
