# sanity

Structural overview of a repository. Every file that git does not ignore is
opened at once as a read-only panel on an infinite canvas, panels arranged by
directory. Zooming out shows the shape of the project, zooming in shows the
code. Changes on disk are picked up live and highlighted.

Tauri 2 shell, Rust backend, WebGL2 renderer. No UI framework.
